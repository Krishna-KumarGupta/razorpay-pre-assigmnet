Prompt 1 — Project Initialization
You are a senior backend engineer.

Create a production-grade backend project using:

- Node.js
- Express.js
- PostgreSQL (local installation)
- Sequelize ORM
- Cookie based authentication
- JWT stored inside HttpOnly cookies

Follow SOLID principles.

Create only the initial project structure.

Do not implement business logic yet.

Create:

src/
├── config
├── controllers
├── services
├── repositories
├── middleware
├── routes
├── models
├── migrations
├── seeders
├── utils
├── validations

Configure:
- Express
- PostgreSQL connection
- Sequelize initialization
- Environment variables
- Error handling middleware

The application must run on port 7002.

Return folder structure and code files.

Prompt 2 — Database Design
Using Sequelize and PostgreSQL.

Create models and migrations for:

User
EmployeeManager
Reimbursement
ReimbursementApproval

Requirements:

User roles:
EMP
RM
APE
CFO

Every employee reports to one RM.

A reimbursement belongs to an employee.

Approval history must be stored separately.

Generate:

- Sequelize models
- Associations
- Migration files

Do not generate controllers or routes.


Prompt 3 — Authentication Module
Implement Authentication Service.

Requirements:

POST /rest/onboardings/register
POST /rest/onboardings/login
POST /rest/onboardings/logout

Constraints:

- Only org.com email domain
- Password hashing using bcrypt
- JWT generation
- JWT stored inside HttpOnly cookie
- Logout clears cookie

Generate:

Repository layer
Service layer
Controller layer
Route layer

Follow SOLID principles.

Do not implement RBAC yet.


Prompt 4 — CFO Seeder
Create Sequelize seed file.

Seed exactly one CFO account.

Email:
cfo@org.com

Password:
CFO#ORG@April2026

Store password as bcrypt hash.

Prevent duplicate seed creation.

Generate seed file only.


Prompt 5 — Role Assignment Service
Implement:

POST /rest/roles/assign

Requirements:

Only CFO can access.

Body:
{
 userId,
 role
}

Allowed roles:

EMP
RM
APE
CFO

Generate:

middleware
service
repository
controller

Follow RBAC principles.


Prompt 6 — Employee Assignment
Implement:

POST /rest/employees/assign
DELETE /rest/employees/assign

Requirements:

Only CFO can access.

Assign EMP to RM.

Validation:

- User must exist
- Employee role must be EMP
- Manager role must be RM
- Employee can have only one RM

Generate repository, service, controller and routes.


Prompt 7 — Reimbursement Creation
Implement:

POST /rest/reimbursements

Requirements:

Only EMP can create reimbursement.

Fields:

title
description
amount

Default status:
PENDING

Generate repository, service, controller and routes.